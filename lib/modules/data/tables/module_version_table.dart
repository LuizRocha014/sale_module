/// Definição de tabela (ex.: Drift `@DataClassName`) quando o banco for modelado.
/// Mantido como referência estrutural do template `create_package`.
class ModuleVersionTablePlaceholder {
  const ModuleVersionTablePlaceholder();
}
